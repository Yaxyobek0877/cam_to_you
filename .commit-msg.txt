feat(v0.1.5): CPU-friendly defaults + system info card + smart hints

Target users without GPU (most PCs) — easier path to streaming.

PREVIEW: adaptive resolution + hardware decode
- GPU encoders (NVENC/QSV/AMF) -> 720p
- CPU encoders (x264/openh264/MF) -> 480p (lighter)
- Added -hwaccel auto: HEVC decode on any GPU (NVIDIA/AMD/Intel iGPU)
- Pure CPU systems: falls back to software decode automatically

NEW API: GetHardwareInfo
- Reports available encoders (NVENC, QSV, AMF, x264, OpenH264, MF)
- hasGpu boolean for quick checks
- bestEncoder for "auto" selection
- recommendation field with user-friendly Uzbek text

DASHBOARD: System status card
- Shows GPU/CPU badge prominently
- All encoders listed with availability checkmarks
- Recommendation text (e.g. "CPU only -> use sub-stream + copy")
- Highlights "auto" mode picks best available

STREAM FORM: CPU recommendations
- Encoder dropdown filters to ONLY available encoders
- Warning card if no GPU: suggests 720p + Copy + sub-stream combo
- "720p tavsiya CPU uchun" label on quality option

TYPES: added h264_amf, libopenh264 to Encoder union

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
