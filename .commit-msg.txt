fix(v0.1.10): block start when stream key is invalid + visual warning

Issue: User's DB has stream_key="live2" from prior corrupt save.
Migration v2 only touched keys starting with "rtmp" so "live2" was left.
User clicks Start → same I/O error. They were confused because UI
didn't warn them.

NEW: Start-time validation in manager.go
- Before launching ffmpeg, validates the stream key shape
- Calls ValidateStreamKeyShape (now exported from service)
- Returns clear error: "live2 is YouTube server path, not key. Edit
  stream and paste real key from YouTube Studio"

NEW: Visual indicator on stream card
- ⚠ "Key noto'g'ri" badge appears when stream key fails validation
- Start button is REPLACED with amber "Key kiriting" button that
  opens the edit modal directly
- User can't accidentally click Start with a broken key — they MUST
  fix it first

Now the bad-key state is visible at a glance, and clicking "Key kiriting"
takes user straight to the form (which has the YouTube instructions
collapsible from v0.1.9).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
