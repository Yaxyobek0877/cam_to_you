fix(v0.1.4): hide CMD windows + wait for ready event + verbose FFmpeg logs

CRITICAL: Hide subprocess console windows (no more black CMD popups)
- New internal/ffmpeg/noconsole_windows.go: SysProcAttr with HideWindow + CREATE_NO_WINDOW
- New internal/camera/noconsole_windows.go: same for ffprobe
- Applied to all exec.Cmd calls:
  - ffmpeg.Runner (preview/streaming subprocess)
  - ffmpeg.Installer.VerifyVersion
  - ffmpeg.DetectEncoders
  - camera.Prober.Probe (ffprobe)
- Cross-platform: noconsole_other.go for Linux/macOS (no-op)

PREVIEW: fix manifestLoadError race condition
- Was: fixed 1.5s timer before hls.js setup (too short for slow cameras)
- Now: wait for backend "ready" event (when m3u8 actually exists)
- hls.js only starts loading once we know file is ready
- Reliable for any camera connection speed

PREVIEW: verbose FFmpeg logs
- preview.go: loglevel raised from "warning" to "info"
- Stops filtering out info-level logs in forwardLogs
- User now sees FFmpeg connection progress, codec info, etc.
- Especially helpful during loading state

TOAST: remove action buttons
- "Ochish" action was triggering Windows shell behavior
- Could explain mysterious File Explorer popups
- Removed Actions array — plain notifications only

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
