fix(v0.1.15): faster restart + camera web UI instructions

REAL YouTube test with user's actual stream key (r60a-yzw7-zype-ebh7-9z43):
- Sub-stream + NVENC + no audio: 30 sec uploaded to YouTube ✓
- Main stream + NVENC + audio: ~4 sec then RTSP EOF ✗
- Dual-input (sub video + main audio): immediate failure ✗
- Sub-stream HEVC copy + synthetic silent audio: FLV/HEVC issues ✗

CONCLUSION: Hikvision camera (running HEVC on sub-stream too) has
session limits that disconnect transcoded streams after ~30 seconds.
We CANNOT fix this from app side without changing camera config.

REAL FIX requires user action in camera web UI:
1. Browser → http://<camera-ip>
2. Login as admin
3. Configuration → Video/Audio → Sub Stream
4. Change Video Encoding from H.265 to H.264
5. Save → Then Cam2You can use Copy encoder = 0% CPU + 100% stable

CHANGES in this release:
- internal/stream/manager.go: restart base delay 2s → 1s
  First 10 retries are 1 second apart (was 2s).
  After that, exponential backoff up to 5 min.
  Goal: minimize gap visibility on YouTube during camera reconnects.

- frontend/src/pages/Streams.tsx: replaced "blue tip" with prominent
  amber warning when encoder != "copy":
  - Explains the session limit issue
  - Step-by-step camera web UI instructions
  - Shows path including config file location
  - Highlights "Copy" + H.264 sub-stream as ASLIDA YECHIM

This is honest documentation of the situation. Auto-restart with 1s
gaps helps but isn't perfect. The only 100% solution is camera-side
config change.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
