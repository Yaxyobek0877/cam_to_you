feat(v0.1.3): full-page preview + Logs page + diagnostic info

UX overhaul: navigation instead of modals

PREVIEW (was modal, now full page):
- New route: /cameras/:id/preview
- Camera click "Jonli korish" navigates to dedicated page
- Full-page video player with controls
- Real-time FFmpeg log feed at bottom (collapsible)
- Diagnostic panel: encoder used, masked RTSP URL, state, log count
- Manual retry button + Escape to go back
- RTSP credentials masked in logs (security)

LOGS PAGE (new):
- New route: /logs + sidebar entry
- Real-time stream + preview + ffmpeg event feed
- Filter by source (stream/preview/ffmpeg/system)
- Filter by level (info/warning/error)
- Pause/resume, auto-scroll toggle, clear
- Global event store (ringbuffer, last 1000 entries)
- Survives navigation between pages

BACKEND - preview events:
- internal/preview/preview.go: full event system
  - EventStarting (with encoder info)
  - EventReady (when first segment ready)
  - EventLog (forwarded FFmpeg logs)
  - EventError, EventStopped
- Background goroutine watches index.m3u8 creation
- Logs go to UI via Wails event bus

FIX: builder.go was using -rw_timeout for ffmpeg
- That option only works for ffprobe, not ffmpeg
- Removed (relies on TCP defaults + Go context)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
