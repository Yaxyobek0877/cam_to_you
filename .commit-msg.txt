fix(v0.1.7): show actual FFmpeg error, not just "exit status N"

User reported: "Ishga tushmadi: exit status 0xffffffffb" — opaque error
that gives no clue WHY the stream failed.

BACKEND fixes:
- builder.go: stream loglevel raised from "warning" to "info"
  Captures more diagnostic detail in log buffer.
  UI still filters info-level via forwardLogs (no flood in Logs page).

- runner.go: new enrichError() function
  When process exits with error, appends last 8 warning/error log lines
  to lastErr. If no warnings, uses last 5 lines verbatim.
  Now user sees: "exit status 1\n\nFFmpeg chiqishi:\n• <actual reason>"

FRONTEND fixes:
- Streams.tsx: new ErrorBlock component
  - Summary line shown directly under stream name
  - "Batafsil ko'rsatish" button reveals full FFmpeg output
  - "Loglarni ko'rish" link navigates to /logs page
  - Multi-line errors render as monospace pre block

Now any stream failure surfaces the root cause without needing CLI debug:
- Wrong stream key → "Server returned 403 Forbidden"
- NVENC issue → "h264_nvenc init failed"
- Network → "Connection refused" / "timed out"

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
